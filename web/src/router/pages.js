import Home from '@/views/Home'

const routes = [
    {
        path: '/',
        name: 'Home',
        component: Home,
        meta: {
        	description: 'offbalはあなたの素晴らしい一日を過ごすためのサポートをしています',
        },
    },
]

export default routes